function sumCalculator(firstNum, secondNum) 
{ 
    return firstNum + secondNum;
}
var firstNum=Number(prompt("Insert first number"));
var secondNum=Number(prompt("Insert second number"));

var total = sumCalculator(firstNum,secondNum);
console.log(total)